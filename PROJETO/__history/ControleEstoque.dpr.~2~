program ControleEstoque;

uses
  Forms,
  Principal in '..\FORMS\Principal.pas' {Form1},
  UDM in '..\FORMS\UDM.pas' {DM: TDataModule};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TDM, DM);
  Application.Run;
end.
